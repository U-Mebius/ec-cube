{#
This file is part of EC-CUBE

Copyright(c) EC-CUBE CO.,LTD. All Rights Reserved.

http://www.ec-cube.co.jp/

For the full copyright and license information, please view the LICENSE
file that was distributed with this source code.
#}
{% for message in app.session.flashbag.get('eccube.front.info') %}
    <div class="ec-cartRole__error">
        <div class="ec-alert-warning">
            <div class="ec-alert-warning__icon"><img src="{{ asset('assets/icon/exclamation-white.svg') }}"></div>
            <div class="ec-alert-warning__text">
                {{ message|trans|nl2br }}
            </div>
        </div>
    </div>
{% endfor %}

{% for message in app.session.flashbag.get('eccube.front.success') %}
    <div class="ec-cartRole__error">
        <div class="ec-alert-warning">
            <div class="ec-alert-warning__icon"><img src="{{ asset('assets/icon/exclamation-white.svg') }}"></div>
            <div class="ec-alert-warning__text">
                {{ message|trans|nl2br }}
            </div>
        </div>
    </div>
{% endfor %}

{% for message in app.session.flashbag.get('eccube.front.danger') %}
    <div class="ec-cartRole__error">
        <div class="ec-alert-warning">
            <div class="ec-alert-warning__icon"><img src="{{ asset('assets/icon/exclamation-white.svg') }}"></div>
            <div class="ec-alert-warning__text">
                {{ message|trans|nl2br }}
            </div>
        </div>
    </div>
{% endfor %}

{% for message in app.session.flashbag.get('eccube.front.error') %}
    <div class="ec-cartRole__error">
        <div class="ec-alert-warning">
            <div class="ec-alert-warning__icon"><img src="{{ asset('assets/icon/exclamation-white.svg') }}"></div>
            <div class="ec-alert-warning__text">
                {{ message|trans|nl2br }}
            </div>
        </div>
    </div>
{% endfor %}

{% for message in app.session.flashbag.get('eccube.front.warning') %}
    <div class="ec-cartRole__error">
        <div class="ec-alert-warning">
            <div class="ec-alert-warning__icon"><img src="{{ asset('assets/icon/exclamation-white.svg') }}"></div>
            <div class="ec-alert-warning__text">
                {{ message|trans|nl2br }}
            </div>
        </div>
    </div>
{% endfor %}
